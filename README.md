# Docker Flask App

## Overview
This project demonstrates how to create a simple Python Flask application, containerize it using Docker, run it as a container, and push the image to Docker Hub.

## Files
- `app.py` - basic Flask application
- `requirements.txt` - Python dependencies
- `Dockerfile` - Docker build instructions

CI/CD retry test
